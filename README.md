# scd30-mqtt
application for a pi with a scd30 air quality sensor to send to mqtt topics

# usage
- `chmod +x *.sh`
- install dependencies: `sudo ./install.sh`
- compile: `./compile.sh`
- for a service: `sudo ./makeservice.sh build/scd30_mqtt`
